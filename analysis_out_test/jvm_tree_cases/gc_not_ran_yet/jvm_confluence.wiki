h2. Выкатывать JAVA_TOOL_OPTIONS
*Нет*

h2. Что сделать сейчас
# GC за окно не отработал: рост до первой сборки ещё не утечка. Limit и G1 не трогайте.
# Наклон крутой. Снимите тот же график ещё через 12 ч без рестарта пода: скорость упала (прогрев или кэш) или держится? Появились ли GC, throttle, рост отклика?

h3. Почему так
* Плеч всегда два. Подов на одном плече: 2.
* Рестарт контейнера: не рестартовал.
* Память в тесте растёт: да.
* CPU throttle: нет.
* Скорость роста считайте после выхода на стабильную динамику, не со старта пода (прогрев и заполнение кэша в этот расчёт не входят).
* GC pause p95 80 мс, порог 250 мс; ниже порога.
* Heap used вырос на 10% за 12 ч, 0.83 %/ч.
* GC ещё не отработал: по одному наклону рано решать, упрёмся ли в SLA. Нужна вторая точка через 12 ч без рестарта пода.
* По введённым цифрам рост в Java heap.

h3. Что перепроверить
* Для оценки времени до OOM нужен текущий процент того же показателя (OldGen или memory usage контейнера).
* За окно GC не отработал: рост до первой сборки ещё не утечка. Нужны heap used до и после GC.
* 10% за 12 ч — крутой наклон, даже если текущий usage ещё низкий. Limit и G1 не трогайте до второй точки.
* Снимите тот же график ещё через 12 ч без рестарта пода. Скорость упала (прогрев или кэш) или держится? Если GC за это время отработал — вставьте heap used до и после сборки. Смотрите, не появились ли throttle и рост отклика.

h3. План: SLA, OOM или плато
* Скорость считайте после выхода на стабильную динамику, не со старта пода. Прогрев и заполнение кэша в этот расчёт не входят.
* Три исхода: упрётся в SLA 80%, будет OOMKilled / упрётся в limit, или выйдет на плато ниже SLA (часто около 75%). Если до SLA 80% больше 30 дней — сейчас не критично.
* Плато vs утечка: несколько циклов GC без рестарта. Если минимум после каждой следующей очистки выше — это не плато, проблема всё ещё есть.
* Смена JAVA_TOOL_OPTIONS, memory request или memory limit перезапустит под: память обнулится принудительно. Неизвестно, сколько ждать повторного выхода на ту же динамику — ретест роста после выкатки настроек нельзя сравнивать с этим прогоном.

h3. Ответы по дереву
|| Вопрос || Ответ ||
| Сколько подов этой АС на одном плече | 2 |
| Почему рестартовал контейнер | не рестартовал |
| Память в тесте растёт (рестарта ещё нет) | да |
| Какой показатель рос | Heap used |
| На сколько процентов он вырос | 10 % |
| За сколько часов | 12 ч |
| За это время GC успел отработать | нет |
| Есть CPU throttle | нет |
| GC pause p95, мс | 80 мс |

h2. Цель и ответы дерева
*Система:* DEMO_CounterAgent
*Pod:* counteragent-demo
*Контейнер:* application
*Подов на одном плече:* 2 (плеч всегда 2)
*Рестарт:* не рестартовал
*CPU throttle:* нет

|| Параметр || Значение ||
| gc_pause_p95_ms | 80.0 |


h2. Рекомендации по JVM-тюнингу
*Система:* DEMO_CounterAgent
*Целевой pod:* counteragent-demo
*Целевой контейнер:* application

h3. Ресурсы и JVM настройки контейнера (актуально для настройки)
* Нет изменений ресурсов или JVM-флагов — смотрите рекомендации ниже.

h3. Контекст рантайма
|| Параметр || Значение ||
| JDK version | N/A |
| Spring Boot version | N/A |
| Heap used (MiB) | N/A |
| OldGen used (MiB) | N/A |
| OldGen capacity (MiB) | N/A |
| GC pause p95 (ms) | 80.0 |
| GC pause p99 (ms) | N/A |
| GC time ratio (%) | N/A |
| Container memory working set (MiB) | N/A |
| Container memory limit (MiB) | 8192 |

h3. Findings
* *[INFO]* heap.metric_missing: Heap used не задан. Рекомендации могут быть менее точными.
** source: ui.metric_input
* *[INFO]* oldgen.metric_missing: OldGen used не задан. Рекомендации по OldGen могут быть менее точными.
** source: ui.metric_input

h3. Рекомендованные Java Tool Options
h4. Рекомендация 1: Критичных изменений JVM не требуется
*Обоснование:* Текущие метрики не показывают давление GC или heap выше порогов.
*Уверенность:* low
*Оценка доказательности:* 65/100
*Оценка риска:* 10/100
*Ожидаемый эффект:* оставить baseline без изменений
*Окно верификации:* следующий профильный прогон нагрузки
*Нужна платформенная эскалация:* No
*План отката:*
** Откат не требуется.


h3. Инженерный план валидации
# Apply recommended JVM options in test/stage environment first. Keep previous values for rollback.
# Run at least one representative load profile and compare with baseline metrics collected before the change.
# If no measurable improvement after the observation window, set status to предыдущая попытка тюнинга не дала эффекта and prepare escalation to dump analysis.

h3. Риски и побочные эффекты изменений
* Any JVM option change can shift latency and throughput profile; always compare SLA/SLO against baseline.
* Rollback plan is mandatory: keep previous JVM flags and resource limits so the team can revert quickly.

h3. Правило эскалации
* If tuning recommendations do not improve GC/heap/memory-pressure metrics in the defined observation window, escalate to development team for heap dump and memory analysis.


h3. Контекст прогона
*Целевой контейнер:* application

h3. Результат анализа
* Findings: 2; рекомендаций: 1.

h3. Предлагаемые изменения (Copy/Paste)
* Копируемую строку JAVA_TOOL_OPTIONS не даём — см. кандидатов в Expand выше.
